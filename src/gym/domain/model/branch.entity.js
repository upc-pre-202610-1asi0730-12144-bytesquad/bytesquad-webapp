export class Branch {
  constructor({ id, name, street, district, city }) {
    this.id       = id;
    this.name     = name;
    this.street   = street;
    this.district = district;
    this.city     = city;
  }
}
